{
  "name": "proxy-example",
  "description": "an example of how to use the proxy client",
  "scripts": {
    "build": "yarn webpack",
    "watch": "yarn webpack --watch",
    "serve": "yarn http-server ./example --port 3000",
    "dev": "concurrently 'yarn watch' 'yarn serve'"
  },
  "dependencies": {
    "@types/url-parse": "^1.4.3",
    "concurrently": "^5.3.0",
    "http-server": "^0.12.3",
    "the-proxy-client": "0.2.0",
    "ts-loader": "^8.0.10",
    "typescript": "^4.0.5",
    "url-parse": "^1.4.7",
    "webpack": "^5.4.0",
    "webpack-cli": "^4.2.0"
  }
}
